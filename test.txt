   <TableContainer>
        <Table sx={{ minWidth: 500 }} aria-label="customized table">
          
          <TableHead>
            <TableRow>
              <TableCell>Departamento</TableCell>
              <TableCell>Ciudad</TableCell>
              <TableCell>Sede</TableCell>
              <TableCell>Valor</TableCell>
            </TableRow>
          </TableHead>

          <TableBody>
            {listSede.map((sede) => (

              <TableRow >

                <TableCell>{sede.departamento}</TableCell>
                <TableCell>{sede.municipio}</TableCell>
                <TableCell>{sede.sede}</TableCell>
                <TableCell>{sede.valor}</TableCell>

              </TableRow>
            ))}
          </TableBody>

        </Table>
      </TableContainer>



        {/* <Select
        value={departamento}
        label="Departamento"
        onChange={(e) => { setDepartamento(e.target.value); }}
      >
        {

          departamentos.default.map((elemento, i) => (
            <MenuItem value={elemento}>
              {elemento.name}
            </MenuItem>
          )
          )

        }
      </Select> */}